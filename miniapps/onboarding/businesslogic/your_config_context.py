from collections import defaultdict


def config_context(device_fqdn, device_context, configparser, onboarding_config):

    # write everything you need to "log" to result afterwards

    return device_context